Workouts!
<br>
<ul>
  <% @workouts.each do |workout| %>
  <li><%= link_to "#{workout.name} #{workout.date}", "/workouts/#{workout.id}" %></li>
  <% end %>
</ul>
<% if current_user.has_role? :trainer %>
Trained Users!
<ul>
  <% @trained_users.each do |user| %>
  <li>Username: <%= link_to(user.name, "/trainers/inspect/#{user.id}") %> | Email: <%= user.email %></li>
  <% end %>
</ul>
Workouts!
<ul>
  <% @workouts_as_trainer.each do |workout| %>
  <li><%= workout.name %> <%= User.find(workout.user_id).name%></li>
  <%end%>
</ul>
<%end%>
